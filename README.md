# Left-Rotation2 This C# program rotates the elements of an integer array to the left by a specified number of positions `d`.
It rearranges the list by moving the first `d` elements to the end and then outputs the rotated array.
